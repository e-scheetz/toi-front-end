import React, { Component } from 'react'
import { Navbar } from 'react-materialize'

class AppHeader extends Component {
  render(){
    return(
      <Navbar brand='Tap On It :: Code Challenge' right>
      </Navbar>
    )
  }
}

export default AppHeader
